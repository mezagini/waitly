export async function saveWaitlistEmail(email: string): Promise<boolean> {
  // TODO: Implement D1 persistence later
  console.log(`Mock DB save: ${email}`);
  
  // Return true to simulate a successful save
  return true;
}
